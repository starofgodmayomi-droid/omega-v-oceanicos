# @omega-v/pluralism

Multi-perspective consensus and decentralized signal convergence matrix for Ω∞v Oceanicos.

Houses `PluralismConvergenceMatrix`, reconciling multiple decentralized streams into a unified consensus verdict with HMAC signature proofs.

## Role

* Filters telemetry streams against noise thresholds (agreement ratio >= 0.66).
* Computes unified consensus verdicts (`PASS` or `DIVERGENT`) and produces verifiable attestation evidence.
