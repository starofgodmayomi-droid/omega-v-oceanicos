{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "OMEGA_SIGNING_KEY",
      "description": "HMAC-SHA256 signing key for /v1/attest (>=32 chars). A dev placeholder is provided in .env.base44-defaults; replace with a real random key for trustworthy attestations.",
      "requiredAtBoot": false
    }
  ]
}
