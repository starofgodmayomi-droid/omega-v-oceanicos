version: '3.8'
services:
  omega-v-cluster:
    build: .
    ports:
      - "4102:4102"
      - "3000:3000"
    command: sh -c "pnpm --filter api run start & pnpm --filter web run start"
    restart: always
