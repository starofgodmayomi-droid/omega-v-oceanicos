version: '3.8'
services:
  api:
    build: .
    ports:
      - "5000:5000"
    command: pnpm run start:api
  web:
    build: .
    ports:
      - "3000:3000"
    command: pnpm run start:web
    depends_on:
      - api
