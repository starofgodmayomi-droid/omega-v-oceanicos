# Base44 dev defaults — placeholders so the app boots without real credentials.
# Listed FIRST in env_file; values in /run/base44/app.env (dashboard secrets) override these.
OMEGA_AUTH_MODE=local
OMEGA_SIGNING_KEY=dev-only-signing-key-placeholder-32-chars-min
OMEGA_READ_TOKEN=dev-read-token-placeholder
OMEGA_ADMIN_TOKEN=dev-admin-token-placeholder-distinct
