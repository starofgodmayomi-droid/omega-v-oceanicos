{
  "name": "omega-v-oceanicos",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "pnpm install",
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "dev": "pnpm --filter @oceanicos/api dev",
    "verify:full": "pnpm build && pnpm test:e2e",
    "test:e2e": "node --experimental-vm-modules node_modules/jest/bin/jest.js --config jest.config.cjs tests/e2e --runInBand",
    "cli": "tsx bin/oceanicos.ts"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "devDependencies": {
    "@jest/globals": "^30.5.1",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.24",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "tsx": "^4.19.2",
    "typescript": "^5.3.3"
  }
}
