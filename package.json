{
  "name": "@omega-v/oceanicos",
  "version": "0.1.0",
  "private": true,
  "description": "A verification-first full-stack ecosystem for observing, verifying, attesting, and continuously evolving trustworthy intelligence systems.",
  "author": "Ω∞v Community",
  "license": "Apache-2.0",
  "homepage": "https://github.com/starofgodmayomi-droid/omega-v-oceanicos",
  "repository": {
    "type": "git",
    "url": "https://github.com/starofgodmayomi-droid/omega-v-oceanicos.git"
  },
  "type": "module",
  "packageManager": "pnpm@8.8.0",
  "pnpm": {
    "overrides": {
      "typescript": "5.3.3"
    }
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --parallel --filter @omega-v/api --filter @omega-v/web dev",
    "build": "pnpm --filter @omega-v/types build && pnpm --filter @omega-v/observer build && pnpm --filter @omega-v/verification build && pnpm --filter @omega-v/remember build && pnpm --filter @omega-v/mini build && pnpm --filter @omega-v/attestation build && pnpm --filter @omega-v/api build && pnpm --filter @omega-v/web build",
    "clean": "pnpm --parallel --filter @omega-v/api --filter @omega-v/web --filter @omega-v/attestation --filter @omega-v/mini --filter @omega-v/remember --filter @omega-v/observer --filter @omega-v/verification --filter @omega-v/types clean && rm -rf coverage",
    "test": "jest",
    "test:fast": "jest --testPathIgnorePatterns=integration",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:integration": "jest --testPathPattern=integration",
    "lint": "eslint . --ext .ts,.tsx,.js,.jsx",
    "lint:fix": "eslint . --ext .ts,.tsx,.js,.jsx --fix",
    "type-check": "tsc --noEmit",
    "format:check": "prettier --check .",
    "format:fix": "prettier --write .",
    "verify": "pnpm lint && pnpm type-check && pnpm test && pnpm build",
    "verify:fast": "pnpm lint && pnpm type-check && pnpm test:fast",
    "verify:full": "pnpm lint && pnpm type-check && pnpm test:coverage && pnpm test:integration && pnpm build",
    "docs:build": "echo 'Documentation build (coming soon)'",
    "docs:test": "echo 'Documentation test (coming soon)'",
    "docs:watch": "echo 'Documentation watch (coming soon)'",
    "precommit": "pnpm lint && pnpm type-check",
    "prepare": "husky install 2>/dev/null || true"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-prettier": "^5.0.0",
    "jest": "^29.5.0",
    "prettier": "^3.0.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
